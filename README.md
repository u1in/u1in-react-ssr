# u1in-react-ssr

React ssr render framework

## Usage:

Install

```shell
npm i

npm run build
```

Run In Client Side Render Mode：

```shell
npm run csr
```

Run In Server Side Render Mode：

```shell
npm run ssr
```
